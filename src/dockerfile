# 1. 选择基础镜像
FROM node:18

# 2. 设置工作目录
WORKDIR /app

# 3. 复制 package.json 和 package-lock.json
COPY package.json ./

# 4. 安装依赖
RUN npm install

# 5. 复制所有代码
COPY . .

# 6. 启动应用
CMD ["npm", "start"]

# 7. 暴露端口
EXPOSE 3000
